#include <stdio.h>
/**
 * main - Entry point
 * Description: prints all the alphabets in lower case
 * Return: return alphabets
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; ++a)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
